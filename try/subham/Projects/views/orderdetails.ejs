<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
        <script src="/javascript/index.js"></script>
        <title><%= title%></title>
        <style>
            body{
                margin-top: 8%;
            }
            .card {
                box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
                transition: 0.3s;
                width: 35%;
                height:400px;
            }
            .card:hover {
                box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2);
            }
            .container {
                padding: 2px 16px;
            }
            .w3-content{
                display:flex;
            }
        </style>
    </head>
    <body>
        <div class="w3-content w3-display-container">
            <%blog.forEach((i)=>{ %>
            <div class="card">
                <div class="container">
                  <h4><b><%= i.name%></b></h4> 
                  <p><%= i.credit_card_number%></p> 
                  <p><%= i.orderdate%></p> 
                </div>
            </div> 
        <% });%>
        <a href="/totalorder/<%= prev%>"><button class="w3-button w3-black w3-display-left">&#10094;</button></a>
        <a href="/totalorder/<%= next%>"><button class="w3-button w3-black w3-display-right">&#10095;</button></a>
        </div>
    </body>
</html>
