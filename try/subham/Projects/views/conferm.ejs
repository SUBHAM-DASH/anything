<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= title%></title>
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <style>
            body{
                text-align: center;
                display:flex;
                justify-content: center;
                align-items: center;
            }
            .card {
                box-shadow: 0 8px 8px 0 rgba(0,0,0,0.2);
                transition: 0.3s;
                width: 35%;
                height:450px;
                margin-top: 60px;
            }
            .card:hover {
                box-shadow: 0 16px 20px 0 rgba(0,0,0,0.2);
            }
            button{
                margin-top: 40px;
                width: 40%;
                height: 30px;
                border:4px;
                border-radius: 20px;
                background-color: yellow;
                font-size: 20px;
            }
            .container{
                color:rgb(243, 71, 8);
            }
        </style>
    </head>
    <body>
        <%user.forEach((i)=>{ %>
        <div class="card">
            <i class="material-icons" style="font-size:70px;color:rgb(7, 199, 7)">person_pin_circle</i>
            <div class="container">
                <h2><b>Ordered Confirmed! &#xe834;</b></h2> 
                <p><%= i.name%></p>
                <p>userid :<%= (userid)%></p>
                <p>orderid :<%= (orderid)%></p>
                <p>ordered date :<%= (todaydate)%></p>  
                <p>delivery date<%= (orderdate)%></p>  
            </div>
            <a href="/home"><button type="button">Back to shop</button></a>
            <% });%>
        </div>
    </body>
</html>
